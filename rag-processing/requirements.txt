# RAG Processing Dependencies
# For GPU support, install PyTorch first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   (or cu118 for CUDA 11.8, or cpu for CPU-only)

# Core dependencies
sentence-transformers>=2.2.0
PyMuPDF>=1.23.0
requests>=2.31.0
python-dotenv>=1.0.0
tqdm>=4.65.0
numpy>=1.24.0

# Optional: For faster tokenization
tokenizers>=0.15.0
